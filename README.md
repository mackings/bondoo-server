# BONDOO Server

Express and TypeScript API for the BONDOO mobile application.

## Commands

```bash
npm install
npm run build
npm start
```

Copy `.env.example` to `.env` for local development. Production secrets are
configured in Render and must not be committed.
